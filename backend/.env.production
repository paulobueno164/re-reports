# Configurações do Servidor
PORT=3030
NODE_ENV=production

# Configurações do Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=re_reports
DB_USER=postgres
DB_PASSWORD=ee1631

# Configurações de JWT
JWT_SECRET=re-reports-production-jwt-secret-2026-secure-key
JWT_EXPIRES_IN=7d

# Configurações de SMTP para envio de emails
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=seu-email@empresa.com
SMTP_PASS=sua-senha-smtp

# Secret para chamadas de CRON jobs
CRON_SECRET=re-reports-cron-secret-2026

# Caminho para armazenamento de arquivos (uploads)
STORAGE_PATH=./uploads
